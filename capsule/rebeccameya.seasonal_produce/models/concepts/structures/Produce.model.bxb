structure (Produce) {
  description (A fruit or vegetable)

  property (name) {
    type (ProduceName)
    min (Required) max (One)
  }
  property (type) {
    type (ProduceType)
    min (Required) max(One)
  }
  property (season) {
    type (Season)
    min (Required) max(Many)
  }
}